// Подсказка "вводить латиницей" — единый стиль для всех анкет (визы +
// брони). Одной красной строкой, без выделений и градиентов, чтобы
// фокус был на самом сообщении а не на форматировании.
//
// Используется один раз на форму — обычно сразу под заголовком блока
// с личными данными ("Пассажир" / "Личные данные" / "Заявитель").
// После добавления Notice — не нужно дублировать "(как в загранпаспорте)"
// в каждом лейбле поля.

export default function LatinNotice({ className = '' }: { className?: string }) {
  return (
    <p className={`text-[12px] text-[#DC2626] leading-snug ${className}`}>
      Данные вводить латиницей, как в загранпаспорте
    </p>
  );
}
